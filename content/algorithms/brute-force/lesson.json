{
  "algorithmSlug": "brute-force",
  "title": "모든 두 수 쌍을 확인해 target 찾기",
  "objective": "이중 반복문의 범위를 정확히 잡아 중복 없이 모든 후보 쌍을 확인한다.",
  "codeLanguage": "python",
  "inputExample": "nums = [1, 3, 4, 2, 5, 6]\ntarget = 7",
  "outputExample": "[(0, 5), (1, 2), (3, 4)]",
  "representativeCode": "def find_two_sum_pairs(nums, target):\n    pairs = []\n    n = len(nums)\n\n    for i in range(n):\n        for j in range(i + 1, n):\n            if nums[i] + nums[j] == target:\n                pairs.append((i, j))\n\n    return pairs",
  "traceSteps": [
    {
      "label": "첫 인덱스 고정",
      "explanation": "바깥 반복문에서 현재 기준이 될 i를 하나 고정한다."
    },
    {
      "label": "뒤쪽 후보 확인",
      "explanation": "안쪽 반복문은 i보다 큰 j만 돌며 중복 없는 쌍을 만든다."
    },
    {
      "label": "조건 만족 시 기록",
      "explanation": "합이 target과 같으면 현재 인덱스 쌍을 결과에 추가한다."
    }
  ],
  "blankExercise": {
    "prompt": "완전탐색은 범위를 빠짐없이 돌되 중복만 막는 것이 핵심이다.",
    "template": "pairs = []\nn = len(nums)\n\n__BLANK_b1__\n    __BLANK_b2__\n        if nums[i] + nums[j] == target:\n            __BLANK_b3__\n\nreturn pairs",
    "blanks": [
      {
        "id": "b1",
        "answer": "for i in range(n):",
        "acceptedAnswers": [
          "foriinrange(n):"
        ],
        "hint": "첫 번째 인덱스를 고정하는 바깥 반복문이다."
      },
      {
        "id": "b2",
        "answer": "for j in range(i + 1, n):",
        "acceptedAnswers": [
          "forjinrange(i+1,n):"
        ],
        "hint": "중복을 막기 위해 i 다음부터 도는 안쪽 반복문이다."
      },
      {
        "id": "b3",
        "answer": "pairs.append((i, j))",
        "acceptedAnswers": [
          "pairs.append((i,j))"
        ],
        "hint": "조건을 만족한 인덱스 쌍을 결과에 저장한다."
      }
    ]
  },
  "parsonsExercise": {
    "prompt": "인덱스 고정 -> 뒤쪽 탐색 -> 조건 만족 시 기록 순서를 복원한다.",
    "lines": [
      "    pairs = []",
      "    n = len(nums)",
      "    for i in range(n):",
      "        for j in range(i + 1, n):",
      "            if nums[i] + nums[j] == target:",
      "                pairs.append((i, j))",
      "    return pairs"
    ],
    "answerOrder": [
      0,
      1,
      2,
      3,
      4,
      5,
      6
    ],
    "groupedIndices": []
  },
  "commonMistake": {
    "title": "j를 0부터 다시 시작해 같은 쌍을 두 번 보는 실수",
    "whyWrong": "완전탐색이라도 중복 기준을 정해야 (0,1)과 (1,0)을 둘 다 세지 않는다."
  },
  "bridgeSummary": "실전 문제에서 더 빠른 풀이를 쓰더라도, 먼저 완전탐색 기준 구현을 만들면 반례와 정답 검증에 큰 도움이 된다."
}
